<tr>
	<td><%= produto.nome %></td>
	<td><%= produto.descricao %></td>
	<td><%= produto.preco %></td>
	<td><%= button_to "Remover", produto, class: "btn btn-danger", method: :delete, data: { confirm: "Tem certeza que deseja remover?" } %></td>
	<td><%= link_to "Alterar", edit_produto_path(produto), class: "btn btn-info" %></td>
</tr>